[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

[build.environment]
  NETLIFY_USE_MISE = "false"
  NETLIFY_SKIP_MISE = "true"
  NODE_VERSION = "22"

# SPA Fallback Redirect for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

